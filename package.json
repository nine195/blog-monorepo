{
  "name": "dev-partner-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,admin,server -c auto \"pnpm -C web dev\" \"pnpm -C admin dev\" \"pnpm -C server start:dev\"",
    "dev:web": "pnpm -C web dev",
    "dev:admin": "pnpm -C admin dev",
    "dev:server": "pnpm -C server start:dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
